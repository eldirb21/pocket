const LANGS = {
  en: {
    CHAT: {},
    SETTINGS: {},
  },
  id: {
    CHAT: {},
    SETTINGS: {},
  },
};

const DEFAULT_LANGUAGE = 'en';

const LABELS = LANGS[DEFAULT_LANGUAGE];

export default LABELS;
